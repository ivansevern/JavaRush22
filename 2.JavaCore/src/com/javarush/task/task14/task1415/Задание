В этой задаче тебе нужно:
    1. Реализовать метод cleanAllApartments().
    2. Пройтись по списку объектов apartments:
        для однокомнатных квартир (OneRoomApt) вызвать метод clean1Room();
        для двухкомнатных квартир (TwoRoomApt) вызвать метод clean2Room();
        для трехкомнатных квартир (ThreeRoomApt) вызвать метод clean3Room();
        использовать instanceOf.



Требования:

Метод cleanAllApartments должен принимать список аппартаментов в качестве параметра.

В методе cleanAllApartments для всех однокомнатных аппартаментов (OneRoomApt) вызвать метод clean1Room();

В методе cleanAllApartments для всех двухкомнатных квартир (TwoRoomApt) вызвать метод clean2Room();

В методе cleanAllApartments для всех трехкомнатных квартир (ThreeRoomApt) вызвать метод clean3Room();

Классы OneRoomApt, TwoRoomApt, ThreeRoomApt должны поддерживать р(реализовывать) интерфейс Apartment.